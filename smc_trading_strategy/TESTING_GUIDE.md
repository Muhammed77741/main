# Testing Guide - Semi-Auto Bot

Руководство по тестированию полуавтоматического бота на демо-счёте.

---

## 📋 Подготовка к тестированию

### 1. Требования:
- ✅ MT5 установлен и запущен
- ✅ Вошли в **DEMO** аккаунт (⚠️ НЕ REAL!)
- ✅ Включен "Algo Trading" в MT5
- ✅ Telegram бот настроен (см. SEMIAUTO_BOT_README.md)
- ✅ Файл `.env` создан с токенами

### 2. Проверка подключения:

```bash
cd smc_trading_strategy
python run_semiauto_bot.py
```

Этот скрипт проверит:
- ✅ Конфигурацию .env
- ✅ Подключение к MT5
- ✅ Подключение к Telegram

---

## 🧪 Тестовые скрипты

### 1. `test_open_position.py` - Простое открытие позиций

#### Примеры использования:

**Показать текущие позиции:**
```bash
python test_open_position.py --action show
```

**Открыть LONG позицию (базовая):**
```bash
python test_open_position.py --action long --lot 0.01 --sl 20 --tp 50
```

**Открыть SHORT позицию:**
```bash
python test_open_position.py --action short --lot 0.01 --sl 20
```

**Открыть LONG с V9 параметрами:**
```bash
python test_open_position.py --action v9-long --lot 0.01
```

**Открыть SHORT с V9 параметрами:**
```bash
python test_open_position.py --action v9-short --lot 0.01
```

**Закрыть все тестовые позиции:**
```bash
python test_open_position.py --action close
```

#### Параметры:
- `--action`: Действие (long, short, v9-long, v9-short, show, close)
- `--lot`: Размер лота (по умолчанию 0.01)
- `--sl`: Stop Loss в пунктах (по умолчанию 20)
- `--tp`: Take Profit в пунктах (0 = управляется ботом)

---

### 2. `test_position_manager.py` - Продвинутое управление

Этот скрипт позволяет:
- Открывать позиции с точными V9 параметрами
- Модифицировать SL/TP
- Делать частичное закрытие
- Мониторить позицию в реальном времени
- Симулировать достижение TP

#### Примеры:

**Открыть LONG V9 TREND:**
```bash
python test_position_manager.py --action open-long --lot 0.01
```
Вывод покажет:
- Ticket позиции
- Параметры (Entry, SL, TP1/TP2/TP3)
- **Tracking data для добавления в бот**

**Открыть SHORT V9 TREND:**
```bash
python test_position_manager.py --action open-short --lot 0.01
```

**Показать детали позиции:**
```bash
python test_position_manager.py --action show --ticket 123456789
```

**Модифицировать SL позиции:**
```bash
python test_position_manager.py --action modify --ticket 123456789 --sl 2520.50
```

**Частичное закрытие (30%):**
```bash
python test_position_manager.py --action partial --ticket 123456789 --volume 0.003
```
*(0.01 lot × 30% = 0.003)*

**Мониторить позицию 120 секунд:**
```bash
python test_position_manager.py --action monitor --ticket 123456789 --duration 120
```

**Симулировать достижение TP1:**
```bash
python test_position_manager.py --action sim-tp --ticket 123456789 --tp-level 1
```
Это закроет 30% позиции (как если бы TP1 был достигнут)

**Симулировать TP2:**
```bash
python test_position_manager.py --action sim-tp --ticket 123456789 --tp-level 2
```

**Симулировать TP3:**
```bash
python test_position_manager.py --action sim-tp --ticket 123456789 --tp-level 3
```

---

## 🔬 Сценарии тестирования

### Сценарий 1: Тест сигнала и подтверждения

**Цель:** Проверить что бот находит сигналы и отправляет в Telegram

**Шаги:**
1. Запустить бота:
   ```bash
   python run_semiauto_bot.py
   ```

2. Подождать сигнала (максимум 1 час)

3. Получить в Telegram:
   ```
   🚨 НОВЫЙ СИГНАЛ V9 🚨
   Signal ID: LONG_1234567890
   ```

4. Подтвердить:
   ```
   /confirm LONG_1234567890
   ```

5. Проверить что позиция открылась в MT5

**Ожидаемый результат:**
- ✅ Сигнал пришёл в Telegram
- ✅ Позиция открылась после /confirm
- ✅ Уведомление "Position Opened" получено
- ✅ В MT5 видна позиция

---

### Сценарий 2: Тест частичного закрытия

**Цель:** Проверить автоматическое частичное закрытие на TP

**Шаги:**
1. Открыть тестовую позицию:
   ```bash
   python test_position_manager.py --action open-long --lot 0.01
   ```

2. Скопировать ticket из вывода (например: 123456789)

3. **ВАЖНО:** Вручную добавить позицию в бот!
   - Останавливаем бота (Ctrl+C)
   - Или используем другой способ (см. ниже)

4. Симулировать TP1:
   ```bash
   python test_position_manager.py --action sim-tp --ticket 123456789 --tp-level 1
   ```

5. Проверить:
   - Закрылось 30% позиции
   - Осталось 70% (0.007 lot из 0.01)

**Ожидаемый результат:**
- ✅ 30% позиции закрылись
- ✅ Бот получил уведомление
- ✅ Telegram уведомление "TP1 HIT!"

---

### Сценарий 3: Тест trailing stop

**Цель:** Проверить что trailing stop обновляется

**Шаги:**
1. Открыть LONG позицию
2. Дождаться движения цены вверх на 35+ пунктов (TP1)
3. Бот должен:
   - Закрыть 30%
   - Переместить SL на breakeven (entry price)
   - Далее обновлять SL каждую минуту (trailing 25п)

**Проверка:**
```bash
# Мониторить каждые 30 секунд
watch -n 30 'python test_open_position.py --action show'
```

Смотреть как SL двигается вслед за ценой.

**Ожидаемый результат:**
- ✅ После TP1: SL = Entry price
- ✅ Цена растёт: SL тоже растёт (отстаёт на 25п)
- ✅ SL никогда не уменьшается (только вверх для LONG)

---

### Сценарий 4: Тест timeout

**Цель:** Проверить что позиция закрывается по таймауту

**Настройка короткого timeout для теста:**

Временно изменить в `live_bot_mt5_semiauto.py`:
```python
# V9 LONG TREND timeout (ORIGINAL: 60)
self.long_trend_timeout = 0.05  # 3 минуты для теста
```

**Шаги:**
1. Открыть позицию
2. Подождать 3 минуты
3. Бот должен закрыть позицию автоматически

**Ожидаемый результат:**
- ✅ Позиция закрылась через 3 минуты
- ✅ Telegram уведомление "Position Closed - TIMEOUT"

**⚠️ НЕ ЗАБУДЬТЕ** вернуть timeout на 60 после теста!

---

### Сценарий 5: Тест отмены сигнала

**Цель:** Проверить что можно отменить сигнал

**Шаги:**
1. Получить сигнал в Telegram
2. Отменить вместо подтверждения:
   ```
   /cancel LONG_1234567890
   ```

**Ожидаемый результат:**
- ✅ Уведомление "Signal Cancelled"
- ✅ Позиция НЕ открылась в MT5
- ✅ Сигнал удалён из pending

---

### Сценарий 6: Тест команд Telegram

**Цель:** Проверить все команды бота

**Команды для теста:**

```
/status
```
Ожидается:
- Баланс, Equity, Margin
- Количество открытых позиций
- Количество pending signals

```
/positions
```
Ожидается:
- Список всех открытых позиций
- Детали каждой (ticket, direction, profit, SL, TP)

```
/help
```
Ожидается:
- Список всех команд
- Краткая инструкция

---

## 🐛 Типичные проблемы при тестировании

### Проблема: "Position not added to bot tracking"

**Причина:** Позиции открытые через test scripts НЕ добавляются в `bot.open_positions` автоматически.

**Решение 1:** Открывать позиции через подтверждение сигналов:
1. Дождаться сигнала от бота
2. Использовать `/confirm`

**Решение 2:** Вручную добавить tracking (для разработки):
```python
# В live_bot_mt5_semiauto.py, добавить в __init__:
# (только для тестирования!)

# Manual test position
test_position = {
    'ticket': 123456789,  # YOUR TICKET
    'direction': 'LONG',
    'regime': 'TREND',
    'entry_price': 2500.00,
    'entry_time': datetime.now(),
    'stop_loss': 2480.00,
    'tp1_price': 2535.00,
    'tp2_price': 2565.00,
    'tp3_price': 2600.00,
    'tp1_hit': False,
    'tp2_hit': False,
    'tp3_hit': False,
    'trailing_distance': 25,
    'timeout_hours': 60,
    'initial_volume': 0.01,
    'current_volume': 0.01,
}
self.open_positions.append(test_position)
```

### Проблема: "Бот не обнаруживает TP hits"

**Причина:** Цена не двигается до TP уровней.

**Решение:** Использовать `sim-tp` для симуляции:
```bash
python test_position_manager.py --action sim-tp --ticket 123456789 --tp-level 1
```

### Проблема: "Partial close failed"

**Возможные причины:**
1. Volume too small (< 0.01)
2. Broker не поддерживает partial close
3. Hedging mode вместо Netting

**Проверка:**
```python
# В MT5 Python console:
import MetaTrader5 as mt5
mt5.initialize()
account = mt5.account_info()
print(f"Margin mode: {account.margin_mode}")
# MARGIN_MODE_RETAIL_NETTING = partial close supported
# MARGIN_MODE_RETAIL_HEDGING = partial close NOT supported
```

---

## ✅ Чеклист перед тестированием

Перед началом тестов убедитесь:

- [ ] MT5 запущен на **DEMO** аккаунте
- [ ] Algo Trading включен
- [ ] XAUUSD доступен в Market Watch
- [ ] .env файл настроен
- [ ] Telegram бот отвечает на /start
- [ ] run_semiauto_bot.py прошёл все проверки
- [ ] Баланс демо-счёта > $500
- [ ] Нет открытых позиций (или все закрыты)

---

## 📊 Логирование результатов

Создайте файл `test_log.txt` для записи результатов:

```
=== TEST SESSION 2024-01-15 ===

Test 1: Signal Detection
Time: 14:30
Result: ✅ Signal received in Telegram
Signal ID: LONG_1705327800
Notes: Took 45 minutes to get signal

Test 2: Position Confirmation
Time: 14:35
Result: ✅ Position opened after /confirm
Ticket: 123456789
Entry: 2500.50
Notes: Opened instantly

Test 3: Partial Close (TP1)
Time: 14:40
Result: ✅ 30% closed automatically
Remaining: 0.007 lot
Notes: Bot detected TP1 within 1 minute

...
```

---

## 🎯 Успешное тестирование

Тест считается успешным если:

1. ✅ Бот находит сигналы
2. ✅ Сигналы приходят в Telegram
3. ✅ `/confirm` открывает позицию в MT5
4. ✅ `/cancel` отменяет сигнал
5. ✅ Частичное закрытие работает (30%/30%/40%)
6. ✅ Trailing stop обновляется после TP1
7. ✅ Timeout закрывает позицию
8. ✅ Все Telegram уведомления приходят
9. ✅ `/status` и `/positions` показывают правильные данные

---

## 🚀 После успешного тестирования

Когда все тесты пройдены на **DEMO**:

1. Протестировать минимум **7 дней** на демо
2. Собрать статистику:
   - Сколько сигналов получено
   - Сколько подтверждено
   - Результаты сделок
3. Проверить стабильность бота (не падает, не зависает)
4. Убедиться что Telegram уведомления приходят всегда

Только после этого можно **ОСТОРОЖНО** тестировать на real с минимальным лотом!

---

## 📞 Поддержка

При возникновении проблем:

1. Проверить логи бота в консоли
2. Проверить `/status` в Telegram
3. Убедиться что MT5 подключен
4. Перезапустить бота

Удачного тестирования! 🧪
