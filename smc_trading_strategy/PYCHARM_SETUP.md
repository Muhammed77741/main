# 🐍 Запуск Live Bot в PyCharm

## 📦 Шаг 1: Открыть проект

1. Открой **PyCharm**
2. **File** → **Open**
3. Выбери папку `smc_trading_strategy`
4. Нажми **OK**

---

## ⚙️ Шаг 2: Настроить интерпретатор Python

### Вариант A: Использовать существующий Python

1. **File** → **Settings** (Ctrl+Alt+S на Windows/Linux или Cmd+, на Mac)
2. **Project: smc_trading_strategy** → **Python Interpreter**
3. Выбери Python 3.8+ из списка
4. Нажми **OK**

### Вариант B: Создать виртуальное окружение (рекомендуется)

1. **File** → **Settings**
2. **Project: smc_trading_strategy** → **Python Interpreter**
3. Нажми значок **⚙️** справа → **Add...**
4. Выбери **Virtualenv Environment**
5. **Location**: оставь по умолчанию
6. **Base interpreter**: выбери Python 3.8+
7. Нажми **OK**

---

## 📥 Шаг 3: Установить зависимости

### Способ 1: Через PyCharm (автоматически)

PyCharm увидит `requirements.txt` и предложит установить зависимости:

1. Открой файл `requirements.txt`
2. В верхней части появится баннер: **"Package requirements 'requirements.txt' are not satisfied"**
3. Нажми **Install requirements**
4. Дождись окончания установки

### Способ 2: Через терминал в PyCharm

1. Открой терминал в PyCharm: **View** → **Tool Windows** → **Terminal** (или Alt+F12)
2. Выполни команду:

```bash
pip install -r requirements.txt
```

### Если будут проблемы с multitasking:

```bash
pip install yfinance --no-deps
pip install pandas numpy requests python-dotenv
```

---

## 🔑 Шаг 4: Настроить Telegram (опционально)

### Создать .env файл:

1. В PyCharm, кликни правой кнопкой на папку `smc_trading_strategy`
2. **New** → **File**
3. Имя файла: `.env`
4. Вставь свои данные:

```env
TELEGRAM_BOT_TOKEN=6851850308:AAEw96lCqeT4W7RINj_iPEkttgNobi9cmNU
TELEGRAM_CHAT_ID=-1002029944880
CHECK_INTERVAL=3600
```

5. Сохрани файл (Ctrl+S)

**Примечание:** Файл уже должен быть создан, если нет - создай как описано выше.

---

## ▶️ Шаг 5: Запустить бота

### Способ 1: Запуск через Run Configuration (рекомендуется)

1. Открой файл `paper_trading.py` в редакторе
2. Кликни правой кнопкой на файле
3. Выбери **Run 'paper_trading'**
   
   Или используй зеленую стрелку ▶️ рядом с `if __name__ == "__main__":`

4. Бот запустится, и ты увидишь вывод в консоли PyCharm

### Способ 2: Через терминал PyCharm

1. Открой терминал: **View** → **Tool Windows** → **Terminal**
2. Убедись что находишься в папке `smc_trading_strategy`:

```bash
cd smc_trading_strategy  # если нужно
python paper_trading.py
```

### Способ 3: С настройками запуска

1. **Run** → **Edit Configurations...**
2. Нажми **+** → **Python**
3. Настрой:
   - **Name**: Live Paper Trading Bot
   - **Script path**: путь к `paper_trading.py`
   - **Working directory**: путь к `smc_trading_strategy`
   - **Environment variables**: (необязательно, если есть .env)
4. Нажми **OK**
5. Теперь можешь запускать через **Run** → **Run 'Live Paper Trading Bot'**

---

## 📊 Что ты увидишь при запуске:

```
================================================================================
🤖 PAPER TRADING BOT SETUP
================================================================================

✅ Paper Trading Bot initialized
   TP Configuration: 30п/50п/80п
   Close %: 50%/30%/20%

================================================================================
🤖 PAPER TRADING BOT STARTED - BASELINE 3TP
================================================================================
⏱️  Check interval: 3600s (1.0h)
📊 Strategy: Pattern Recognition (1.618)
📈 Asset: XAUUSD (Gold Futures - GC=F)
🎯 TP Levels: 30п / 50п / 80п
   Close %: 50% / 30% / 20%
================================================================================

🔍 Checking for signals at 2025-12-28 10:00:00
================================================================================
📥 Downloading GC=F data...
✅ Downloaded 120 candles
   Latest: 2025-12-28 09:00:00 | Price: 4327.50
```

---

## 🛑 Остановка бота

### В PyCharm:

1. Нажми красную кнопку **Stop** (■) в панели запуска
   
   Или используй **Ctrl+F2**

2. Бот корректно завершится:

```
⚠️  Bot stopped by user
📊 Final stats: 5 trades, 1 open
```

---

## 🐛 Отладка (Debugging)

Если хочешь пошагово отслеживать работу:

1. Открой `paper_trading.py`
2. Кликни слева от номера строки, чтобы поставить **breakpoint** (красная точка)
   
   Хорошие места для breakpoint:
   - `def check_for_signals(self):` - когда проверяется новый сигнал
   - `def open_position(self, signal, timestamp):` - когда открывается позиция
   - `def check_open_positions(self):` - когда проверяются TP/SL

3. Кликни правой кнопкой на файле → **Debug 'paper_trading'** (или Shift+F9)
4. Бот остановится на breakpoint, и ты сможешь:
   - Смотреть значения переменных
   - Выполнять код пошагово (F8 - следующая строка, F7 - войти в функцию)

---

## 📝 Настройки для комфортной работы

### Включить автосохранение:

1. **File** → **Settings** → **Appearance & Behavior** → **System Settings**
2. Включи **Save files automatically if application is idle for X sec**

### Показать консоль всегда:

1. **Run** → **Edit Configurations...**
2. Выбери свою конфигурацию
3. Включи **Show console when a message is printed to standard output**

---

## ⚙️ Продвинутые настройки

### Изменить частоту проверки (без перезапуска):

Измени в `.env`:
```env
CHECK_INTERVAL=1800  # 30 минут (вместо 3600)
```

Перезапусти бота в PyCharm.

### Запуск в фоне:

В терминале PyCharm:
```bash
nohup python paper_trading.py > bot.log 2>&1 &
```

Для остановки:
```bash
pkill -f paper_trading.py
```

---

## 🎯 Быстрые клавиши PyCharm

| Действие | Клавиша |
|----------|---------|
| Запустить | Shift+F10 |
| Остановить | Ctrl+F2 |
| Отладка | Shift+F9 |
| Терминал | Alt+F12 |
| Найти файл | Ctrl+Shift+N |
| Найти в файлах | Ctrl+Shift+F |

---

## ✅ Checklist перед запуском

- [ ] Python 3.8+ установлен
- [ ] Виртуальное окружение создано (рекомендуется)
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Файл `.env` создан с Telegram данными
- [ ] Файл `paper_trading.py` открыт в PyCharm
- [ ] Интернет-соединение активно

---

## 🚀 Готово!

Теперь можешь:
1. ▶️ Запустить бота
2. 📱 Получать уведомления в Telegram
3. 📊 Наблюдать за сделками в консоли PyCharm
4. 🐛 Отлаживать при необходимости

**Удачной торговли! 💰**
