# 🚀 Quick Start Guide - Live Trading Bot

## Для Запуска Live Бота (русский)

### 1. Убедитесь что у вас есть:
- ✅ Windows (MT5 работает только на Windows)
- ✅ MetaTrader 5 установлен и работает
- ✅ Python 3.8+

### 2. Установите зависимости:
```bash
cd smc_trading_strategy
pip install -r requirements.txt
```

### 3. Настройте `.env` файл:
```bash
# Скопируйте пример
copy .env.example .env

# Отредактируйте .env в текстовом редакторе
```

Минимальная конфигурация:
```bash
# Telegram (опционально, но рекомендуется)
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id

# Проверка каждый час
CHECK_INTERVAL=3600

# Мониторинг позиций каждые 30 секунд
MONITOR_INTERVAL=30
```

### 4. Запустите бот:
```bash
python.exe .\run_fullauto_bot.py
```

или через PowerShell:
```powershell
python .\run_fullauto_bot.py
```

### 5. Что будет происходить:
- ✅ Бот подключится к MT5
- ✅ Каждый час проверяет новые сигналы
- ✅ Записывает все сделки в `trade_logs/`
- ✅ Каждые 30 секунд проверяет позиции в MT5
- ✅ Отслеживает TP1, TP2, TP3 и Stop Loss
- ✅ Отправляет уведомления в Telegram

### 6. Логи и файлы:
Все сделки сохраняются в `trade_logs/`:
- `live_trades.json` - полная история
- `live_trades.csv` - для Excel
- `open_positions.json` - текущие позиции

### 7. Остановка:
Нажмите `Ctrl+C` в терминале

---

## Quick Test (without MT5)

Хотите протестировать без MT5? Запустите симуляцию:
```bash
python test_full_system.py
```

Это создаст 3 тестовые сделки и покажет как работает система.

---

## Troubleshooting

### Ошибка: "MT5 initialization failed"
1. Убедитесь что MT5 запущен
2. Проверьте что используете Windows
3. Перезапустите MT5

### Ошибка: "ModuleNotFoundError"
```bash
pip install -r requirements.txt
```

### Telegram не работает
1. Проверьте токен и chat_id в `.env`
2. Убедитесь что бот добавлен в канал
3. Проверьте интернет

### Позиции не обнаруживаются
1. Откройте позицию в MT5 по XAUUSD
2. Проверьте что MT5 подключен к брокеру
3. Проверьте логи бота

---

## Детальная Документация

Полная документация: [README_FULLAUTO_BOT.md](README_FULLAUTO_BOT.md)

---

## Support

Если возникли проблемы:
1. Проверьте логи бота в консоли
2. Проверьте файлы в `trade_logs/`
3. Запустите `test_full_system.py` для проверки
4. Проверьте что MT5 работает правильно

**Успешной торговли! 📈💰**
