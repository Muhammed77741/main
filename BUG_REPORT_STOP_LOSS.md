# 🐛 КРИТИЧЕСКАЯ ОШИБКА: Аномальные Stop Loss

## Проблема

В майских бэктестах обнаружены сделки с **аномально большими stop loss**:

| Дата | Цена | Stop Loss | SL Distance | SL % |
|------|------|-----------|-------------|------|
| 05-01 | 2309.73 | **2488.53** | 178.80 | **7.74%** ❌ |
| 05-06 | 2322.69 | **2690.85** | 368.16 | **15.85%** ❌❌ |
| 05-08 | 2305.56 | **2795.61** | 490.05 | **21.26%** ❌❌❌ |
| 05-13 | 2335.90 | **2791.45** | 455.55 | **19.50%** ❌❌❌ |
| 05-27 | 2355.84 | **3305.73** | 949.39 | **40.30%** ❌❌❌❌ |

**Нормальный SL должен быть 0.5-2%, НЕ 21-40%!**

## Корневая причина

**Файл:** `smc_trading_strategy/simplified_smc_strategy.py`
**Строка:** 269
**Метод:** `_calculate_stop_loss()`

```python
# НЕПРАВИЛЬНО! ❌
else:  # Short
    recent_swings = df[df['swing_high']].iloc[max(0, idx-20):idx]
    if len(recent_swings) > 0:
        stop = recent_swings['high'].max()  # ← ПРОБЛЕМА!
```

### Что происходит:

1. Берутся все swing highs за последние **20 свечей**
2. Выбирается **МАКСИМАЛЬНЫЙ** (`.max()`)
3. Если в окне есть старый swing high на уровне 2795, а цена 2305 - SL = 2795!

### Пример:

```
Текущая цена: 2305.56
Swing highs в окне 20 свечей:
  - 2024-04-20: 2795 (старый максимум)
  - 2024-05-06: 2332 (недавний)

Текущий код: stop = max(2795, 2332) = 2795  ❌ ОШИБКА!
Правильно:    stop = 2332 (ближайший)       ✅
```

## Влияние

Это объясняет почему **МАЙ 2024** показал **+181% прибыли**:

- Сделки закрывались по SL с **ПРИБЫЛЬЮ +40%** вместо убытка
- Например: entry=2355, SL=3305, но цена пошла вверх и "достигла SL" с прибылью!
- **Это артефакт кода, а не реальная торговля!**

## Решение

### Вариант 1: Использовать ПОСЛЕДНИЙ swing high (рекомендуется)

```python
else:  # Short
    recent_swings = df[df['swing_high']].iloc[max(0, idx-20):idx]
    if len(recent_swings) > 0:
        stop = recent_swings['high'].iloc[-1]  # Последний (ближайший)
    else:
        stop = df['high'].iloc[max(0, idx-10):idx].max()

    return stop * 1.002
```

### Вариант 2: Использовать swing high за последние N свечей (более консервативно)

```python
else:  # Short
    # Ищем ближайший swing high (не старше 10 свечей)
    recent_swings = df[df['swing_high']].iloc[max(0, idx-10):idx]
    if len(recent_swings) > 0:
        stop = recent_swings['high'].iloc[-1]
    else:
        stop = df['high'].iloc[max(0, idx-5):idx].max()

    return stop * 1.002
```

### Вариант 3: Фиксированный % от цены

```python
else:  # Short
    # SL на 0.5-1% выше цены
    entry = df['close'].iloc[idx]
    stop = entry * 1.005  # 0.5% риск
    return stop
```

## Действия

1. ✅ Найдена ошибка в коде
2. ⏳ Исправить `_calculate_stop_loss()` метод
3. ⏳ Перезапустить все бэктесты с исправленным кодом
4. ⏳ Сравнить новые результаты с текущими

## Ожидаемый эффект

После исправления:
- **Май 2024** НЕ будет показывать +181% (это был артефакт)
- Винрейт может СНИЗИТЬСЯ (потому что сейчас много "ложных" выигрышей)
- Результаты будут **РЕАЛИСТИЧНЫМИ**, а не завышенными

---

**Дата обнаружения:** 2026-01-03
**Серьезность:** КРИТИЧЕСКАЯ
**Статус:** Найдена, ожидает исправления
