# Мониторинг Позиций в Реальном Времени - Реализация

## Что было сделано

Реализован **мониторинг позиций в реальном времени** для бота `run_fullauto_bot.py`, который проверяет уровни Take Profit (TP) и записывает все детали в отдельный файл.

## Основные функции

### ✅ 1. Непрерывный мониторинг позиций
- Проверка всех открытых позиций каждые **10 секунд**
- Работает даже между часовыми проверками сигналов
- Никакие срабатывания TP не пропускаются

### ✅ 2. Отслеживание трех уровней TP
Бот отслеживает три разных уровня TP для каждой позиции:
- **TP1**: Консервативная цель (TREND: 30p, RANGE: 20p)
- **TP2**: Умеренная цель (TREND: 55p, RANGE: 35p)  
- **TP3**: Агрессивная цель (TREND: 90p, RANGE: 50p)

### ✅ 3. Детальное логирование
Когда срабатывает TP, в файл `bot_tp_hits_log.csv` записываются:

| Поле | Описание |
|------|----------|
| Timestamp | Точная дата и время срабатывания TP |
| Ticket | Номер ордера MT5 |
| TP_Level | Какой TP сработал (TP1, TP2 или TP3) |
| Type | Тип позиции (BUY или SELL) |
| Volume | Размер позиции в лотах |
| Entry_Price | Цена входа в позицию |
| TP_Target | Целевая цена TP |
| Current_Price | Фактическая цена при обнаружении TP |
| SL | Уровень Stop Loss |
| Profit | Расчетная прибыль в валюте счета |
| Pips | Прибыль в пунктах |
| Market_Regime | Режим рынка (TREND или RANGE) |
| Duration_Minutes | Сколько времени заняло достижение TP (в минутах) |
| Comment | Комментарий позиции (например, V3_T_TP1) |

## Файлы логов

Бот создает два отдельных файла:

1. **`bot_trades_log.csv`**: Полная история сделок (когда позиции полностью закрыты)
2. **`bot_tp_hits_log.csv`**: События срабатывания TP в реальном времени (НОВОЕ!)

### Пример лога срабатываний TP

```csv
Timestamp,Ticket,TP_Level,Type,Volume,Entry_Price,TP_Target,Current_Price,SL,Profit,Pips,Market_Regime,Duration_Minutes,Comment
2026-01-07 10:15:30,123456,TP1,BUY,0.01,2650.00,2680.00,2680.50,2640.00,30.00,30.0,TREND,45.5,V3_T_TP1
2026-01-07 11:30:15,123457,TP2,BUY,0.01,2650.00,2705.00,2705.20,2640.00,55.00,55.0,TREND,120.3,V3_T_TP2
2026-01-07 14:45:00,123458,TP3,SELL,0.01,2680.00,2630.00,2629.80,2690.00,50.00,50.0,RANGE,180.0,V3_R_TP3
```

## Как это работает

### Логика обнаружения

Бот использует систему обнаружения с допуском (tolerance):

**Для BUY позиций:**
- TP считается сработавшим когда: `текущая_цена >= (цель_tp - допуск)`
- Допуск: 0.5 пунктов (для учета проскальзывания)

**Для SELL позиций:**
- TP считается сработавшим когда: `текущая_цена <= (цель_tp + допуск)`
- Допуск: 0.5 пунктов (для учета проскальзывания)

### Интеграция с ботом

Мониторинг интегрирован на двух уровнях:

1. **Во время проверки сигналов** (каждый час):
   - Проверяет уровни TP сразу после проверки новых сигналов
   - Гарантирует обнаружение срабатываний TP, произошедших прямо в час

2. **Во время периодов ожидания** (между часами):
   - Непрерывный мониторинг каждые 10 секунд
   - Это основной компонент мониторинга в реальном времени

## Telegram уведомления

Когда срабатывает TP, бот отправляет Telegram уведомление (если настроено) с:
- 🎯 Оповещение TP HIT
- Номер тикета
- Уровень TP (TP1, TP2 или TP3)
- Тип позиции и цена входа
- Целевой TP vs текущая цена
- Объем и рассчитанные пункты

## Использование

### Запуск бота

```bash
# Запуск в реальном режиме
python run_fullauto_bot.py

# Запуск в тестовом режиме (без реальных сделок)
python run_fullauto_bot.py --dry-run
```

### Вывод в консоли

Когда срабатывает TP, вы увидите:
```
🎯 TP HIT LOGGED: Ticket=123456, Level=TP1, Price=2680.50, Pips=30.5
```

### Просмотр логов

```bash
# Просмотр всех срабатываний TP
cat bot_tp_hits_log.csv

# Подсчет срабатываний по уровням
grep "TP1" bot_tp_hits_log.csv | wc -l
grep "TP2" bot_tp_hits_log.csv | wc -l
grep "TP3" bot_tp_hits_log.csv | wc -l

# Просмотр последних срабатываний TP
tail -n 10 bot_tp_hits_log.csv
```

## Анализ производительности

### Использование лога срабатываний TP

```python
import pandas as pd

# Загрузить лог
df = pd.read_csv('bot_tp_hits_log.csv')

# 1. Частота срабатываний по уровням
print(df['TP_Level'].value_counts())

# 2. Среднее время до срабатывания TP
avg_duration = df.groupby('TP_Level')['Duration_Minutes'].mean()
print(f"Среднее время до TP:\n{avg_duration}")

# 3. Производительность по режиму рынка
regime_stats = df.groupby(['Market_Regime', 'TP_Level']).size()
print(f"Срабатывания по режиму рынка:\n{regime_stats}")

# 4. Общая прибыль по уровням TP
profit_by_level = df.groupby('TP_Level')['Profit'].sum()
print(f"Общая прибыль по уровням:\n{profit_by_level}")
```

## Тестирование

Предоставлен тестовый скрипт для проверки функциональности мониторинга:

```bash
python test_tp_monitoring.py
```

Проверяет:
- ✅ Создание файла лога срабатываний TP
- ✅ Правильные заголовки CSV
- ✅ Функциональность логирования срабатываний TP
- ✅ Отслеживание нескольких уровней TP
- ✅ Логика обнаружения для BUY/SELL

## Технические детали

### Измененные файлы

1. **`smc_trading_strategy/live_bot_mt5_fullauto.py`**:
   - Добавлен `tp_hits_file` для логирования
   - Добавлен метод `_initialize_tp_hits_log()`
   - Добавлен метод `_log_tp_hit()` для записи срабатываний
   - Добавлен метод `_check_tp_levels_realtime()` для мониторинга
   - Изменен метод `_wait_until_next_hour()` для включения мониторинга
   - Обновлена информация о конфигурации при запуске

2. **`test_tp_monitoring.py`** (НОВЫЙ):
   - Комплексный набор тестов для проверки функциональности

3. **`TP_MONITORING_README.md`** (НОВЫЙ):
   - Подробная документация на английском языке

4. **`FULLAUTO_BOT_README.md`**:
   - Обновлен раздел Features
   - Обновлен раздел How It Works
   - Обновлен раздел Logging

## Преимущества

1. **Прозрачность**: Точное знание, когда срабатывает каждый уровень TP
2. **Анализ производительности**: Анализ, какие уровни TP работают лучше в разных рыночных условиях
3. **Обновления в реальном времени**: Немедленные уведомления при достижении целей
4. **Детальная история**: Полные записи для бэктестинга и оптимизации

## Дальнейшие улучшения

Возможные улучшения:
- [ ] Настраиваемый интервал мониторинга
- [ ] Регулируемый допуск для каждого уровня TP
- [ ] Email уведомления
- [ ] Дашборд статистики срабатываний TP
- [ ] Автоматическое частичное закрытие на основе срабатываний TP

## Документация

- [TP_MONITORING_README.md](TP_MONITORING_README.md) - Полная документация (English)
- [FULLAUTO_BOT_README.md](FULLAUTO_BOT_README.md) - Основная документация бота
- [QUICK_START_RU.md](QUICK_START_RU.md) - Быстрый старт (Русский)
