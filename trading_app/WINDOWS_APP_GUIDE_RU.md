# 🪟 Полное руководство по Windows-приложению с лицензированием

## 📋 Обзор реализации

Реализована полная система Windows-приложения с профессиональным лицензированием:

### ✅ Что реализовано:

#### 1️⃣ Устанавливаемое Windows-приложение
- ✅ Сборка в формат .exe (PyInstaller, оптимизированный)
- ✅ Скрипт для создания установщика (Inno Setup)
- ✅ Ярлыки на рабочем столе и в меню Пуск
- ✅ Иконка приложения (поддержка добавления)
- ✅ Версия и метаданные приложения
- ✅ Работа на Windows 10/11

#### 2️⃣ Оптимизированная структура
- ✅ Production-готовая структура проекта
- ✅ Разделение: бизнес-логика / UI / лицензирование / ресурсы
- ✅ Исключение из сборки: тесты, временные файлы
- ✅ Оптимизация размера билда (исключение ненужных модулей)
- ✅ Быстрый запуск (режим onedir)

#### 3️⃣ Trial-версия на 7 дней
- ✅ Trial начинается с первого запуска
- ✅ Дата первого запуска и окончания - в зашифрованном виде
- ✅ Хранение в ProgramData (вне папки приложения)
- ✅ Проверка срока при каждом запуске
- ✅ Отображение оставшихся дней
- ✅ Блокировка после истечения trial

#### 4️⃣ Активация по лицензионному ключу
- ✅ Проверка ключа локально (без сервера)
- ✅ Алгоритмическая валидация (RSA-based)
- ✅ Ключ не хранится в открытом виде
- ✅ Сохранение статуса активации
- ✅ Блокировка при неверном ключе
- ✅ UI для активации

#### 5️⃣ Безопасность
- ✅ Шифрование данных лицензии (Fernet)
- ✅ Привязка к устройству (HWID)
- ✅ Защита от удаления файлов (множественные копии, скрытые)
- ✅ Защита от изменения даты системы
- ✅ Проверка целостности данных

#### 6️⃣ Инструменты
- ✅ Python + PySide6 (оптимальный для Windows)
- ✅ PyInstaller (onedir для быстрого запуска)
- ✅ Inno Setup (скрипт готов)
- ✅ Шифрование (Fernet + RSA)
- ✅ GUI инструмент для генерации ключей

## 🚀 Быстрый старт

### Шаг 1: Установка зависимостей

```bash
cd trading_app
pip install -r requirements.txt
```

### Шаг 2: Сборка приложения

```bash
python build_enhanced.py
```

Этот скрипт автоматически:
- Соберёт оптимизированный .exe
- Создаст release-пакет
- Сгенерирует скрипт установщика
- Выведет инструкции

### Шаг 3: Тестирование

```bash
cd release/TradingBotManager
TradingBotManager.exe
```

При первом запуске увидите окно активации с двумя опциями:
1. Начать 7-дневный trial
2. Активировать по лицензионному ключу

## 🔑 Система лицензирования

### Trial-версия (7 дней)

**Как работает:**
1. При первом запуске показывается диалог активации
2. Пользователь может выбрать "Начать 7-дневный trial"
3. Данные trial шифруются и сохраняются в:
   ```
   %PROGRAMDATA%\TradingBotManager\.config\.trial.dat
   %PROGRAMDATA%\TradingBotManager\.config\.tbm.cfg (резервная копия)
   ```
4. При каждом запуске проверяется срок
5. Статус trial показывается в главном окне
6. После истечения требуется активация

**Защита trial:**
- ✅ Шифрование Fernet с ключом на основе HWID
- ✅ Привязка к Hardware ID (нельзя скопировать на другой ПК)
- ✅ Обнаружение манипуляций с системным временем
- ✅ Множественные места хранения (если удалить один файл, восстановится из резервной копии)
- ✅ Скрытые файлы (атрибут Windows)

### Активация по лицензионному ключу

**Процесс активации:**

1. **Пользователь получает Hardware ID:**
   - Открывает приложение
   - В диалоге активации видит свой Hardware ID
   - Копирует его и отправляет вам

2. **Вы генерируете лицензионный ключ:**
   ```bash
   python tools/keygen_tool.py
   ```
   - Открывается GUI инструмент
   - Вставляете Hardware ID клиента
   - Выбираете срок (7 дней / 1 год / Lifetime)
   - Выбираете функции (ALL / BTC / ETH / XAUUSD)
   - Генерируете ключ
   - Отправляете ключ клиенту

3. **Пользователь вводит ключ:**
   - Открывает диалог активации
   - Вводит полученный ключ
   - Нажимает "Активировать"
   - При успехе приложение разблокируется

**Формат лицензионного ключа:**
```
XXXXX-XXXXX-XXXXX-XXXXX-XXXXX
Пример: AB12C-34DE5-6FG78-9HIJ0-12K3L
```

**Особенности:**
- ✅ Привязан к Hardware ID (один ключ = один ПК)
- ✅ Алгоритмическая валидация (работает без интернета)
- ✅ Lifetime или с ограничением по времени
- ✅ Разные наборы функций (ALL, BTC, ETH, XAUUSD)
- ✅ Безопасность на основе RSA

## 🛠️ Генерация лицензионных ключей

### Метод 1: GUI инструмент (рекомендуется)

```bash
python tools/keygen_tool.py
```

В открывшемся окне:
1. Вставьте Hardware ID клиента
2. Выберите срок действия:
   - 7 days (Trial)
   - 30 days
   - 90 days
   - 180 days
   - 365 days (1 год)
   - 730 days (2 года)
   - 1095 days (3 года)
   - Lifetime (без срока)
3. Выберите функции:
   - ALL (Full Access) - все боты
   - BTC Only - только Bitcoin
   - ETH Only - только Ethereum
   - XAUUSD Only - только Gold
4. Нажмите "Generate License Key"
5. Скопируйте и отправьте ключ клиенту

### Метод 2: Python скрипт

```python
from licensing import generate_license_key, get_hardware_id

# Hardware ID клиента
hwid = "PASTE_CUSTOMER_HWID_HERE"

# Генерация ключа на 1 год, все функции
license_key = generate_license_key(hwid, days=365, features="ALL")
print(f"License Key: {license_key}")

# Генерация lifetime ключа
license_key_lifetime = generate_license_key(hwid, days=0, features="ALL")
print(f"Lifetime Key: {license_key_lifetime}")

# Только BTC на 90 дней
btc_key = generate_license_key(hwid, days=90, features="BTC")
print(f"BTC Key: {btc_key}")
```

## 📦 Сборка и распространение

### Сборка приложения

```bash
cd trading_app
python build_enhanced.py
```

**Что создаётся:**
- `dist/TradingBotManager/` - Файлы приложения
- `release/TradingBotManager/` - Готовый пакет для распространения
- `release/README.txt` - Инструкция для пользователя
- `resources/installer/setup.iss` - Скрипт установщика

### Создание установщика (опционально)

1. Установите Inno Setup:
   https://jrsoftware.org/isinfo.php

2. Откройте `resources/installer/setup.iss` в Inno Setup

3. Нажмите "Compile"

4. Установщик создастся в:
   `resources/installer/Output/TradingBotManager_Setup_v1.0.0.exe`

**Что даёт установщик:**
- Автоматическая установка в Program Files
- Ярлык на рабочем столе
- Ярлык в меню Пуск
- Правильное удаление через "Программы и компоненты"
- Проверка совместимости с Windows 10/11

### Способы распространения

**Вариант A: Папка (проще)**
- Архивируйте `release/TradingBotManager/`
- Отправьте клиенту
- Клиент распаковывает и запускает `.exe`

**Вариант B: Установщик (профессиональнее)**
- Отправьте `TradingBotManager_Setup_v1.0.0.exe`
- Клиент запускает установщик
- Всё устанавливается автоматически

## 🔒 Детали безопасности

### Что защищено

1. **Trial данные:**
   - Дата начала trial
   - Дата окончания trial
   - Hardware ID
   - Зашифрованы Fernet с ключом на основе HWID

2. **Лицензия:**
   - Лицензионный ключ
   - Hardware ID
   - Информация о лицензии
   - Зашифрованы Fernet с ключом на основе HWID

### Механизмы защиты

1. **Защита от удаления файлов:**
   ```
   - Файлы хранятся в двух местах
   - При отсутствии основного файла восстанавливается из резервной копии
   - Файлы скрыты (атрибут Windows)
   ```

2. **Защита от изменения времени:**
   ```python
   # Проверка при каждом запуске
   if current_time < trial_start_time:
       # Обнаружена манипуляция временем
       return False
   ```

3. **Привязка к устройству:**
   ```python
   # Hardware ID генерируется из:
   - MAC адреса
   - CPU ID (через WMIC)
   - Serial номера материнской платы
   - Хешируется SHA256
   ```

4. **Шифрование:**
   ```python
   # Trial и лицензия шифруются Fernet
   # Ключ генерируется из HWID + соль
   # Невозможно расшифровать на другом ПК
   ```

### Что НЕ защищено (и как улучшить)

**Текущий уровень:** Средний (достаточно для большинства случаев)

**Для повышения защиты можно добавить:**
1. **Обфускация кода** (PyArmor):
   ```bash
   pip install pyarmor
   pyarmor obfuscate --recursive main.py
   ```

2. **Online проверка лицензии** (периодическая):
   - Раз в неделю проверять лицензию на сервере
   - Обнаруживать использование одного ключа на разных ПК

3. **Anti-debugging**:
   ```python
   import ctypes
   if ctypes.windll.kernel32.IsDebuggerPresent():
       sys.exit(1)
   ```

## 🧪 Тестирование

### Тест 1: Trial система

```bash
# 1. Запустите приложение первый раз
python main.py

# 2. Должен показаться диалог активации
# 3. Нажмите "Start 7-Day Trial"
# 4. Приложение должно запуститься
# 5. В верхней части должен показываться статус trial
```

**Проверка файлов trial:**
```
Windows: %PROGRAMDATA%\TradingBotManager\.config\
Файлы: .trial.dat, .tbm.cfg (скрытые)
```

### Тест 2: Активация лицензии

```bash
# 1. Запустите приложение
# 2. В диалоге активации скопируйте Hardware ID
# 3. Запустите keygen_tool:
python tools/keygen_tool.py

# 4. Вставьте Hardware ID, сгенерируйте ключ
# 5. Вернитесь в приложение, введите ключ
# 6. Нажмите "Activate License"
# 7. Должно активироваться успешно
```

### Тест 3: Защита от копирования

```bash
# 1. Найдите файлы лицензии:
%PROGRAMDATA%\TradingBotManager\.config\.lic.dat

# 2. Скопируйте на другой ПК или виртуальную машину
# 3. Запустите приложение
# 4. Должна быть ошибка "License is for different machine"
```

### Тест 4: Защита от манипуляции временем

```bash
# 1. Запустите trial
# 2. Измените дату системы на неделю назад
# 3. Запустите приложение
# 4. Должна быть ошибка "System time manipulation detected"
```

## 🐛 Решение проблем

### Проблема: "PyInstaller not found"
```bash
pip install pyinstaller
```

### Проблема: "Module 'cryptography' not found"
```bash
pip install cryptography
```

### Проблема: Приложение не запускается
1. Запустите от имени администратора
2. Проверьте антивирус (может блокировать)
3. Убедитесь что Windows 10/11 64-bit

### Проблема: "License validation failed"
- Проверьте что Hardware ID совпадает
- Убедитесь что ключ введён правильно
- Не копируйте лицензию с другого ПК

### Проблема: Trial сбрасывается
- НЕ удаляйте файлы в %PROGRAMDATA%\TradingBotManager\
- НЕ меняйте системное время
- НЕ копируйте на другой ПК

## 📋 Структура файлов

### Исходный код

```
trading_app/
├── main.py                          # Точка входа с лицензированием
├── gui/
│   ├── main_window.py               # Главное окно (с статусом лицензии)
│   ├── activation_dialog.py         # 🆕 Диалог активации
│   └── ...
├── licensing/                       # 🆕 Система лицензирования
│   ├── __init__.py
│   ├── license_manager.py           # Главный менеджер
│   ├── trial_manager.py             # Trial система
│   ├── keygen.py                    # Генерация/валидация ключей
│   └── hwid.py                      # Hardware ID
├── tools/                           # 🆕 Инструменты
│   └── keygen_tool.py               # GUI для генерации ключей
├── resources/                       # 🆕 Ресурсы
│   ├── icons/                       # Иконки
│   │   └── app_icon.ico             # (добавьте свою)
│   └── installer/                   # Установщик
│       └── setup.iss                # Скрипт Inno Setup
├── build_enhanced.py                # 🆕 Улучшенная сборка
├── requirements.txt                 # Зависимости (обновлено)
├── WINDOWS_APP_GUIDE.md             # 🆕 Документация (ENG)
└── WINDOWS_APP_GUIDE_RU.md          # 🆕 Эта документация (RUS)
```

### После сборки

```
trading_app/
├── dist/
│   └── TradingBotManager/           # Собранное приложение
│       ├── TradingBotManager.exe    # Основной .exe
│       ├── _internal/                # Библиотеки
│       └── ...
├── release/
│   ├── TradingBotManager/           # Готовый пакет
│   │   ├── TradingBotManager.exe
│   │   └── ...
│   └── README.txt                   # Инструкция для пользователя
└── resources/installer/Output/
    └── TradingBotManager_Setup.exe  # Установщик (если создан)
```

## 📞 Поддержка пользователей

### Что предоставить клиентам

1. **README.txt** (включён в release):
   - Инструкция по установке
   - Как начать trial
   - Как активировать лицензию
   - Настройка API ключей
   - Решение проблем

2. **Контактная информация**:
   - Email для поддержки
   - Сайт
   - Инструкция по покупке лицензии

3. **FAQ**:
   - Что такое Hardware ID?
   - Могу ли я перенести лицензию?
   - Что делать если trial истёк?
   - Как получить лицензию?

## 💡 Рекомендации

### Для разработчика

1. **Храните приватный ключ в безопасности**:
   - Ключ в `licensing/keygen.py`
   - Не публикуйте в открытом доступе
   - Делайте backup

2. **Ведите учёт лицензий**:
   - Записывайте: Hardware ID, лицензионный ключ, срок, клиент
   - Это поможет при поддержке
   - Используйте таблицу Excel или БД

3. **Процесс продажи**:
   ```
   1. Клиент покупает → получает инструкцию
   2. Клиент устанавливает приложение
   3. Клиент отправляет Hardware ID
   4. Вы генерируете и отправляете ключ
   5. Клиент активирует
   ```

4. **Политика переноса лицензий**:
   - Определите, можно ли переносить на другой ПК
   - Если да - сколько раз в год
   - Процесс: деактивация старого → активация нового Hardware ID

### Для пользователей (включите в README)

1. **Trial период**:
   - 7 дней полного функционала
   - Начинается с первого запуска
   - Не сбрасывается удалением файлов

2. **Покупка лицензии**:
   - Установите приложение
   - Получите Hardware ID из диалога активации
   - Свяжитесь с поддержкой
   - Получите и введите ключ

3. **Важные правила**:
   - Не меняйте системное время
   - Не удаляйте файлы приложения
   - Один ключ = один ПК
   - Храните ключ в безопасности

## 🎯 Следующие шаги

### 1. Добавить иконку (опционально)

Создайте или скачайте .ico файл и поместите в:
```
trading_app/resources/icons/app_icon.ico
```

Инструменты для создания:
- Online: convertio.co, icoconvert.com
- Offline: GIMP, IcoFX

### 2. Настроить метаданные

Отредактируйте в `build_enhanced.py`:
- Имя приложения
- Версию
- Автора
- URL сайта

### 3. Создать LICENSE.txt

Для установщика нужен файл лицензионного соглашения:
```
trading_app/LICENSE.txt
```

### 4. Протестировать на чистой системе

Рекомендуется тестирование на:
- Чистой Windows 10
- Чистой Windows 11
- Виртуальной машине

### 5. Настроить процесс поддержки

- Email для вопросов
- База знаний / FAQ
- Система учёта лицензий

## ✅ Чеклист релиза

Перед распространением проверьте:

- [ ] Приложение собирается без ошибок
- [ ] Trial система работает корректно
- [ ] Активация лицензии работает
- [ ] Hardware ID генерируется правильно
- [ ] Лицензионные ключи валидируются
- [ ] Защита от копирования работает
- [ ] Защита от манипуляции временем работает
- [ ] Приложение запускается на чистой Windows 10/11
- [ ] Иконка установлена (если нужна)
- [ ] README.txt обновлён
- [ ] Контактная информация актуальна
- [ ] Установщик создан и протестирован (если используется)
- [ ] Есть процесс генерации ключей
- [ ] Есть процесс поддержки клиентов

## 🎉 Готово!

Теперь у вас есть полноценное Windows-приложение с:
- ✅ Trial версией на 7 дней
- ✅ Системой лицензионных ключей
- ✅ Защитой от копирования
- ✅ Профессиональной сборкой
- ✅ Установщиком для Windows
- ✅ Полной документацией

**Удачи в продажах! 🚀**

---

**Разработано специально для профессионального распространения Windows-приложений**
