# GitHub Actions для Stock Screener

Автоматический запуск screener'а через GitHub Actions - **полностью бесплатно**.

## 🎯 Что это дает?

- ✅ **Автоматический запуск** каждый день (или по вашему расписанию)
- ✅ **Бесплатно** - 2000 минут/месяц (хватает на ~60 запусков в день)
- ✅ **Без VPS** - все работает на серверах GitHub
- ✅ **Результаты сохраняются** - можно скачать или закоммитить
- ✅ **Логи всех запусков** - видно что и когда запускалось

## 📋 Доступные workflows

### 1. `stock_screener.yml` - Полнофункциональный
- Пытается запустить real_data_screener.py (с реальными данными)
- Если не получается - запускает demo_screener.py
- Сохраняет результаты как artifacts
- Опционально коммитит результаты в репозиторий

### 2. `screener_simple.yml` - Простой
- Запускает demo_screener.py (без внешних зависимостей)
- Быстро и надежно
- Всегда работает

## 🚀 Быстрый старт

### Шаг 1: Проверить что workflows добавлены
```bash
ls .github/workflows/
# Должны быть:
# - stock_screener.yml
# - screener_simple.yml
```

### Шаг 2: Запушить в GitHub
```bash
git add .github/workflows/
git commit -m "Add GitHub Actions workflows"
git push origin claude/simplify-stock-screener-WzlXB
```

### Шаг 3: Включить Actions
1. Зайти в ваш репозиторий на GitHub
2. Settings → Actions → General
3. Выбрать "Allow all actions and reusable workflows"
4. Сохранить

### Шаг 4: Первый запуск
1. Перейти на вкладку **Actions**
2. Выбрать "Daily Stock Screener" или "Simple Stock Screener"
3. Нажать **Run workflow** → **Run workflow**

### Шаг 5: Посмотреть результаты
1. Дождаться завершения (1-2 минуты)
2. Открыть запуск
3. Скачать **Artifacts** → results

## 📅 Расписание

По умолчанию запускается **каждый день в 9:00 UTC**.

### Изменить расписание:

Открыть `.github/workflows/stock_screener.yml` и изменить cron:

```yaml
on:
  schedule:
    - cron: '0 9 * * *'  # Измените здесь
```

### Примеры расписаний:

```yaml
# Каждый день в 14:00 UTC
- cron: '0 14 * * *'

# Каждый понедельник в 9:00 UTC
- cron: '0 9 * * 1'

# Каждые 6 часов
- cron: '0 */6 * * *'

# Будни в 9:00 и 17:00 UTC
- cron: '0 9,17 * * 1-5'

# Первое число каждого месяца в 10:00 UTC
- cron: '0 10 1 * *'
```

**Генератор расписаний:** https://crontab.guru/

## 📊 Просмотр результатов

### Вариант 1: Artifacts (рекомендуется)
1. GitHub → Actions → Выбрать запуск
2. Artifacts → Скачать CSV
3. Открыть в Excel/Google Sheets

### Вариант 2: Коммит в репозиторий
Результаты автоматически коммитятся в репозиторий (если включено).

1. Перейти в `main/stock_smc_trading/`
2. Найти файл `*_screener_results.csv`
3. Посмотреть историю коммитов

### Вариант 3: Логи
1. GitHub → Actions → Выбрать запуск
2. Развернуть "Run screener"
3. Увидеть весь вывод программы

## 🔧 Настройка

### Отключить автоматический коммит результатов:

В `stock_screener.yml` закомментировать секции:
```yaml
# - name: Commit and push results
#   run: ...
#
# - name: Push changes
#   uses: ...
```

### Запускать только demo_screener.py:

В `stock_screener.yml` изменить:
```yaml
- name: Run screener
  run: |
    cd main/stock_smc_trading
    python3 demo_screener.py  # Вместо real_data_screener.py
```

### Добавить уведомления в Telegram:

```yaml
- name: Send to Telegram
  env:
    TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
    TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
  run: |
    curl -X POST \
      "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
      -d "chat_id=${TELEGRAM_CHAT_ID}" \
      -d "text=Stock screener completed!"
```

Добавить secrets:
1. Settings → Secrets and variables → Actions
2. New repository secret
   - Name: `TELEGRAM_BOT_TOKEN`
   - Value: ваш токен бота
3. Еще один secret:
   - Name: `TELEGRAM_CHAT_ID`
   - Value: ваш chat ID

## 💰 Лимиты и стоимость

### Бесплатный план:
- **2000 минут/месяц** для публичных репозиториев
- **500 MB** хранилище artifacts (30 дней)

### Реальное использование:
- 1 запуск screener'а ≈ 2-3 минуты
- Можно запускать **~30 раз в день** бесплатно
- Или 1 раз в день в течение месяца

### Проверить использование:
Settings → Billing → Usage this month

## 🔍 Monitoring

### Email уведомления:
GitHub автоматически отправляет email если workflow упал.

### Смотреть статус:
```
https://github.com/Muhammed77741/main/actions
```

### Badge в README:
```markdown
![Stock Screener](https://github.com/Muhammed77741/main/actions/workflows/stock_screener.yml/badge.svg)
```

## 🐛 Troubleshooting

### Workflow не запускается автоматически:

**Причина:** Нужна активность в репозитории.
**Решение:** Сделать любой коммит или запустить вручную.

### yfinance не работает:

**Причина:** Внешние запросы могут блокироваться.
**Решение:** Использовать `demo_screener.py` или `comprehensive_screener.py`.

### Artifacts не сохраняются:

**Причина:** Файл не создался или неправильный путь.
**Решение:** Проверить логи, добавить `ls -la` перед upload.

### Permission denied при push:

**Причина:** Нет прав на запись.
**Решение:** Settings → Actions → General → Workflow permissions → Read and write

## 📚 Дополнительные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Cron Schedule Generator](https://crontab.guru/)
- [Awesome Actions](https://github.com/sdras/awesome-actions)

## ✅ Checklist

- [ ] Workflows добавлены в `.github/workflows/`
- [ ] Запушено в GitHub
- [ ] Actions включены в Settings
- [ ] Первый ручной запуск успешен
- [ ] Результаты видны в Artifacts
- [ ] Расписание настроено
- [ ] Email уведомления работают

## 🎉 Готово!

Теперь ваш screener работает автоматически, полностью бесплатно, без VPS!

**Проверить:** https://github.com/Muhammed77741/main/actions
