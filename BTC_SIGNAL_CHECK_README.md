# 📊 BTC Signal Analysis - Quick Start Guide

## Проблема / Problem
У вас есть Windows приложение, которое запускает BTC, ETH, XAUUSD боты. Вы хотите проверить, были ли позиции по BTC за последние 2 дня, и понять, генерируются ли сигналы вообще за последние 7 дней.

**You have a Windows app running BTC, ETH, XAUUSD bots. You want to check if there were any BTC positions in the last 2 days and understand if signals are being generated at all in the last 7 days.**

## Решение / Solution
Скрипт `check_btc_signals.py` анализирует исторические данные BTC и показывает все сигналы, которые были бы сгенерированы ботом.

**The `check_btc_signals.py` script analyzes historical BTC data and shows all signals that would have been generated by the bot.**

---

## 🚀 Быстрый старт / Quick Start

### Вариант 1: Анализ реальных данных с Binance (рекомендуется)
**Option 1: Analyze real data from Binance (recommended)**

```bash
# Проверить BTC за последние 7 дней
# Check BTC for last 7 days
python check_btc_signals.py --days 7

# Проверить ETH за последние 7 дней
# Check ETH for last 7 days
python check_btc_signals.py --symbol ETH/USDT --days 7

# Проверить за последние 14 дней
# Check last 14 days
python check_btc_signals.py --days 14
```

### Вариант 2: Анализ из CSV файла (если нет интернета)
**Option 2: Analyze from CSV file (if no internet)**

```bash
# Если у вас есть CSV файл с историей BTC
# If you have a CSV file with BTC history
python check_btc_signals.py --file BTC_1H_data.csv
```

### Вариант 3: Тестовый режим (для проверки работы скрипта)
**Option 3: Test mode (to verify script works)**

```bash
# Генерирует тестовые данные и анализирует их
# Generates test data and analyzes it
python check_btc_signals.py --sample --days 7
```

---

## 📋 Что показывает скрипт / What the script shows

Скрипт создаёт 3 файла / Script creates 3 files:

1. **`btc_signal_report.csv`** - полная таблица со всеми данными и индикаторами
   - Full table with all data and indicators

2. **`btc_signal_report_signals_only.csv`** - только строки с сигналами
   - Only rows with signals

3. **`btc_signal_report_chart.png`** - график с отметками сигналов
   - Chart with signal markers

### Пример вывода / Example output:
```
📋 Summary:
   Total signals in 7 days: 3
   Signals in last 2 days: 0
   ⚠️  No signals in last 2 days
   Last signal: 2026-01-08 15:00:00
   Days ago: 2
```

---

## 🔍 Интерпретация результатов / Interpreting Results

### ✅ Если сигналы есть в последние 2 дня
**If there ARE signals in last 2 days**
```
✅ Bot IS generating signals normally!

📊 Recent signals:
   2026-01-09 14:00 - BUY 📈 @ $95432.50
   2026-01-10 08:00 - SELL 📉 @ $96123.75
```
**Вывод:** Бот работает нормально, сигналы генерируются.
**Conclusion:** Bot is working normally, signals are being generated.

### ⚠️ Если сигналов НЕТ в последние 2 дня, но есть за 7 дней
**If there are NO signals in last 2 days, but there are in 7 days**
```
⚠️  No signals in last 2 days
Last signal: 2026-01-06 09:45:12
Days ago: 4

💡 This could mean:
   - Market is in consolidation (no clear patterns)
   - Volatility is too low
   - No breakouts or reversals detected
```
**Вывод:** Бот работает, но рынок не подходит для входа (консолидация, нет паттернов).
**Conclusion:** Bot is working, but market conditions don't meet entry criteria (consolidation, no patterns).

### ❌ Если сигналов НЕТ вообще за 7 дней
**If there are NO signals at all in 7 days**
```
❌ NO signals in entire 7-day period!

💡 Possible reasons:
   - Strategy filters are too strict
   - Market conditions don't meet criteria
   - Check strategy settings (fib_mode, tolerances)
```
**Вывод:** Возможно, фильтры стратегии слишком строгие, или рынок действительно не давал сигналов.
**Conclusion:** Possibly strategy filters are too strict, or the market really didn't provide signals.

---

## 🛠️ Установка зависимостей / Install Dependencies

```bash
pip install ccxt pandas numpy matplotlib python-dotenv
```

Или / Or:
```bash
pip install -r trading_bots/crypto_bot/requirements_crypto.txt
```

---

## 📖 Параметры запуска / Command Line Parameters

| Параметр / Parameter | Описание / Description | Пример / Example |
|---------------------|------------------------|------------------|
| `--days N` | Количество дней для анализа / Number of days to analyze | `--days 14` |
| `--symbol SYMBOL` | Торговая пара / Trading symbol | `--symbol ETH/USDT` |
| `--file FILE` | CSV файл с данными / CSV file with data | `--file btc_data.csv` |
| `--sample` | Генерировать тестовые данные / Generate test data | `--sample` |
| `--output PREFIX` | Префикс выходных файлов / Output file prefix | `--output my_report` |

---

## 💡 Часто задаваемые вопросы / FAQ

### Q: Скрипт показывает "NO SIGNALS in last 2 days" - это проблема?
**Q: Script shows "NO SIGNALS in last 2 days" - is this a problem?**

**A:** Не обязательно. Это может быть нормально, если:
- Рынок в боковике (consolidation)
- Нет чётких паттернов (no clear patterns)
- Низкая волатильность (low volatility)

Проверьте, были ли сигналы за последние 7 дней. Если да - бот работает нормально.

**A:** Not necessarily. This can be normal if:
- Market is in consolidation
- No clear patterns
- Low volatility

Check if there were signals in the last 7 days. If yes - bot is working normally.

### Q: Как понять, что бот сломан?
**Q: How to know if the bot is broken?**

**A:** Бот сломан, если:
1. НЕТ сигналов за последние 14+ дней
2. График показывает явные тренды и паттерны, но сигналов нет
3. Бот раньше генерировал 2-3 сигнала в неделю, а сейчас 0

**A:** Bot is broken if:
1. NO signals for last 14+ days
2. Chart shows clear trends and patterns, but no signals
3. Bot used to generate 2-3 signals per week, now 0

### Q: Сколько сигналов нормально за 7 дней?
**Q: How many signals is normal for 7 days?**

**A:** Для BTC/ETH на 1H:
- **0-2 сигнала** = нормально в спокойном рынке (normal in quiet market)
- **3-5 сигналов** = активный рынок (active market)
- **6+ сигналов** = очень волатильный рынок (very volatile market)

**A:** For BTC/ETH on 1H:
- **0-2 signals** = normal in quiet market
- **3-5 signals** = active market
- **6+ signals** = very volatile market

---

## 🎯 Следующие шаги / Next Steps

1. **Запустите скрипт** для BTC и ETH
   **Run the script** for BTC and ETH

2. **Проверьте график** `btc_signal_report_chart.png`
   **Check the chart** `btc_signal_report_chart.png`

3. **Откройте CSV** в Excel для детального анализа
   **Open CSV** in Excel for detailed analysis

4. **Если сигналов нет 14+ дней** - напишите в поддержку с файлами CSV и PNG
   **If no signals for 14+ days** - contact support with CSV and PNG files

---

## 📞 Поддержка / Support

Если скрипт показывает проблему, приложите к сообщению:
**If script shows a problem, attach to your message:**

1. Вывод скрипта в консоли (скриншот или текст)
   Console output (screenshot or text)

2. Файл `btc_signal_report_chart.png`
   File `btc_signal_report_chart.png`

3. Файл `btc_signal_report_signals_only.csv`
   File `btc_signal_report_signals_only.csv`

4. Период анализа (например, "последние 7 дней")
   Analysis period (e.g., "last 7 days")

---

**Создано:** 2026-01-10
**Версия:** 1.0
**Файл:** `check_btc_signals.py`
